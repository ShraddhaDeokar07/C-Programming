#include<stdio.h>
#include<conio.h>

int main()
{
    printf("\n The size of Opretor is %d",sizeof(char));
    printf("\n The size of Opretor is %d",sizeof(int));
    printf("\n The size of Opretor is %d",sizeof(float));
    printf("\n The size of Opretor is %d",sizeof(void));

    getch();
    return 0;
}
